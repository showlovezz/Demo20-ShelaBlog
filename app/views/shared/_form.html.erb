<%= form_for @post do |f| %>
	<%= f.label :title, "Title" %>
	<%= f.text_field :title %>

	<%= f.label :body, "Body" %>
	<%= f.text_field "body" %>

	<%= f.label :description, "Description" %>
	<%= f.text_field :description %>

	<%= f.submit %>
<% end %>